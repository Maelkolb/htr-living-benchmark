# Copy to .env. Only needed to make new runs or to run the judge; scoring the archived runs needs no key.
GOOGLE_API_KEY=
ANTHROPIC_API_KEY=
# ANTHROPIC_WORKSPACE_ID=   # only for an Anthropic key that is tied to a workspace
